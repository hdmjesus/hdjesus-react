import React from 'react';
import { BrowserRouter, Route, Switch } from 'react-router-dom';

const App = () => (
  <BrowserRouter>
    <Switch>
      <Route path='/' component={} />
      <Route component={} />
    </Switch>
  </BrowserRouter>
);

export default App;
